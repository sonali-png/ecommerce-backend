<div class="col-sm-10">
  <div class="col-sm-10">
    <table class="table table-hover">
      <thead>
        <tr>
          <th scope="col">ID</th>
          <th scope="col">Name</th>
          <th>Email</th>
          <th>Role</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% usersList.forEach((user) => { %>
          <tr>
            <td><%= user._id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= (user.role === 0 ? 'Customer' : (user.role === 1 ? "Admin" : "Seller")) %></td>
            <td><i class="bi bi-pencil-square"></i> | <i class="bi bi-trash3"></i></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <nav aria-label="Page navigation example">
      <ul class="pagination justify-content-center">
        <% if (page > 1) { %>
          <li class="page-item">
            <a class="page-link" href="<%= page === 2 ? '/products' : '?page=' + (page - 1) %>">Previous</a>
          </li>
        <% } %>

        <% for (let i = 1; i <= pages; i++) { %>
          <li class="page-item <%= page === i ? 'active' : '' %>">
            <a class="page-link" href="<%= i === 1 ? '/products' : '?page=' + i %>"><%= i %></a>
          </li>
        <% } %>

        <% if (page < pages) { %>
          <li class="page-item">
            <a class="page-link" href="?page=<%= page + 1 %>">Next</a>
          </li>
        <% } %>

      </ul>
    </nav>
  </div>
</div>
  